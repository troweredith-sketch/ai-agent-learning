# DEVLOG

## 2026-04-10

### 本次目标
初始化学生管理系统项目，搭建基础目录结构，准备进入类设计阶段。

### 已完成
- 创建 `src/`、`tests/`、`docs/` 目录
- 初始化 `.gitignore`
- 创建 `README.md`
- 创建 `DESIGN.md`、`DEVLOG.md`、`TESTLOG.md`、`CHANGELOG.md`

### 当前想法
- 使用 Python 面向对象实现控制台版学生管理系统
- 先设计 `Student` 和 `StudentManager`
- 先做最小可运行版本，再逐步扩展

### 下一步
- 完成任务一：明确类职责和整体设计


## 2026-04-10

### 本次目标
完成 Student 类第一版设计与实现。

### 已完成
- 新增 Student 类
- 定义 student_id、name、age、gender、score 五个属性
- 使用 `__str__` 作为学生对象的展示方式
- 明确 Student 只表示单个学生，不负责管理学生列表

### 设计想法
- 第一版先完成“创建对象”和“展示对象”
- 不在 Student 中处理学号重复问题
- 不在当前阶段加入复杂输入校验

### 遇到的问题
- 一开始在 `show_info()` 和 `__str__()` 之间犹豫
- 最终选择 `__str__()`，因为直接 `print(对象)` 更自然

### 下一步
- 开始设计 StudentManager 的内部结构

## 2026-04-10

### 本次目标
完成 StudentManager 第一版设计与实现。

### 已完成
- 新增 StudentManager 类
- 使用列表保存所有 Student 对象
- 实现按学号查找学生
- 实现添加学生功能
- 实现删除学生功能
- 实现修改学生功能
- 实现获取全部学生功能

### 设计想法
- 使用 `find_student_by_id()` 作为基础方法，避免重复编写查找逻辑
- 添加学生时先检查学号是否重复
- 修改学生时仅更新传入的新值
- 第一版先返回 `True / False` 表示操作结果

### 当前状态
- Student 和 StudentManager 的基础对象层已完成
- 下一步准备实现主程序菜单并串联管理功能

### 下一步
- 在 main 中实现菜单交互
- 手动测试添加、删除、修改、查看流程

## 2026-04-10

### 本次目标
完成 main.py 的菜单框架设计与实现。

### 已完成
- 新增主菜单显示函数
- 实现添加、查看、查找、修改、删除的处理函数
- 使用 while True 构建主循环
- 根据用户输入分发到不同功能
- 在输入处理中使用 `strip()` 去除首尾空白

### 设计想法
- main.py 只负责交互与功能调度
- StudentManager 负责业务逻辑
- 使用 `strip()` 提高输入稳定性，避免因为多余空格导致查找、删除、修改失败

### 下一步
- 进行完整流程手动测试
- 优化异常输入处理